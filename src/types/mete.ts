export interface User {
  id: number;
  name: string;
  email: string;
}
export interface BarcodeRef {
  id: string;
  drink: number;
}

export interface Drink {
  name: string;
  id: number;
  price: string;
  logo_url: string;
}
